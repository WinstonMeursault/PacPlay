{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/bin/main",
            "args": [],
            "preLaunchTask": "Make",
            "cwd": "${workspaceFolder}"
        }
    ]
}